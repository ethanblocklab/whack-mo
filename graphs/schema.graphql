type MoWhacked @entity(immutable: true) {
  id: Bytes!
  player: Bytes! # address
  points: BigInt! # uint256
  newTotalPoints: BigInt! # uint256
  blockNumber: BigInt!
  blockTimestamp: BigInt!
  transactionHash: Bytes!
}

type Leaderboard @entity {
  id: Bytes!
  points: BigInt! # uint256
}
